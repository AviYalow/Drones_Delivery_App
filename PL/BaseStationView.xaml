<Window x:Class="PL.BaseStationView" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:controls="clr-namespace:Microsoft.Toolkit.Wpf.UI.Controls;assembly=Microsoft.Toolkit.Wpf.UI.Controls"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
         xmlns:materialdesignin="http://materialdesigninxaml.net/winfx/xaml/themes"
         Icon="/DroneIcon.png" ResizeMode="NoResize"
        Title="BaseStationView" Height="750" Width="1250">
    
    <Window.Background>
        <ImageBrush  ImageSource="BaseStationView.jpg" />

    </Window.Background>
    <Grid Margin="899,0,0,454">

        <controls:MapControl x:Name="mapControl" DockPanel.Dock="Top" ZoomInteractionMode="GestureAndControl"
    TiltInteractionMode="GestureAndControl"  MapServiceToken="EnterYourAuthenticationKeyHere" Margin="-6,-6,6,6" />

        <Label Name="serialNumberLabel" Content="sirialNumber:" HorizontalAlignment="Left" Margin="43,264,0,0" VerticalAlignment="Top" Foreground="#DDFFFFFF"/>
        <Label Name="NameLabel" Content="Name:" HorizontalAlignment="Left" Margin="52,306,0,0" VerticalAlignment="Top" Foreground="#DDFFFFFF"/>
        <Label Name="NumberChargingStationLabel" Content=" Charging Stations:" HorizontalAlignment="Left" Margin="27,345,0,0" VerticalAlignment="Top" Foreground="#DDFFFFFF"/>
        <Label Name="LatitudeLabel" Content="Latitude:" HorizontalAlignment="Left" Margin="37,453,0,0" VerticalAlignment="Top" Foreground="#DDFFFFFF"/>
        <Label Name="LongitudeLabel" Content="Longitude:" HorizontalAlignment="Left" Margin="38,397,0,0" VerticalAlignment="Top" Foreground="#DDFFFFFF"/>
        <TextBox x:Name="SerialText" HorizontalAlignment="Left" Margin="161,262,0,0" Text="{Binding Path=SerialNum,Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Foreground="White" BorderBrush="#89FFFFFF"/>
        <TextBox x:Name="NameText" HorizontalAlignment="Left" Margin="163,302,0,0" Text="{Binding Path=Name,Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Foreground="White" BorderBrush="#89FFFFFF"/>
        <TextBox x:Name="FreeStateText" HorizontalAlignment="Left" Margin="162,344,0,0" Text="{Binding Path=FreeState,Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Foreground="White" BorderBrush="#89FFFFFF"/>
        <TextBox x:Name="Latitudtext" IsEnabled="{Binding ElementName=SerialText, Path=IsEnabled}" HorizontalAlignment="Left" Margin="164,390,0,0" Text="{Binding Path=Latitude,Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Foreground="White" BorderBrush="#89FFFFFF"/>
        <TextBox x:Name="LongitudeText" IsEnabled="{Binding ElementName=SerialText, Path=IsEnabled}" HorizontalAlignment="Left" Margin="163,454,0,0" Text="{Binding Path=Longitude,Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Foreground="White" BorderBrush="#89FFFFFF"/>
        <Button x:Name="AddButton" Content="Button" IsEnabled="{Binding ElementName=SerialText, Path=IsEnabled}" HorizontalAlignment="Left" Margin="176,599,0,0" VerticalAlignment="Top" Click="AddButton_Click"/>
    </Grid>
</Window>
